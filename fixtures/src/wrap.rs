//! `T081`'s soft-wrap fixture — a committed replacement for the CP-1 investigation's uncommitted `/tmp/soft-wrap-fixture.rs` (see `tapes/README.md`'s "does the `↪` continuation marker read on a real captured frame" section, and `tapes/_soft-wrap-check.tape`'s own header, which named this exact gap: "a purpose-built fixture" instead of ~500 scripted `Down` presses to reach a long line buried past the first screenful.
pub const NOTE: &str = "one line, reachable at the top of the file with zero scrolling, long enough to wrap at every calibrated width in tapes/_dimensions.tape (80/100/120/200 columns) so the `↪` marker and the line-number gutter's absence on continuation rows are both visible without touching a single arrow key";
